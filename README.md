# vms-services
 vms services
### Build and run the application:
```
  mvn spotless:apply
  
  mvn clean package
  
  mvn spring-boot:run
  
  ```
  http://localhost:8080/api/swagger-ui
  http://localhost:8080/swagger-ui.html